
# Allow quicker access to folders I link to in the shortcut folder ~/sc
if [ -n "$BASH_VERSION" ]; then
	export CDPATH="~/sc";
elif [ -n "$ZSH_VERSION" ]; then
	[[ -d ~/sc ]] && export cdpath=(. ~/sc)
fi;

